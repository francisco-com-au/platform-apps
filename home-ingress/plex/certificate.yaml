apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: plex
  namespace: dev
spec:
  dnsNames:
    - plex.francisco.com.au
  secretName: plex-tls
  issuerRef:
    name: letsencrypt-cluster-issuer
    kind: ClusterIssuer