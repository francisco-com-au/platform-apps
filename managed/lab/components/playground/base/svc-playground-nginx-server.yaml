---
kind: "Service"
apiVersion: "v1"
metadata:
  name: "playground-nginx-server"
  namespace: lab
spec:
  selector:
    app: lab
    component: playground
    container: nginx
  ports:
    - name: "server"
      protocol: "TCP"
      port: 80
