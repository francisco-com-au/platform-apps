---
kind: "Service"
apiVersion: "v1"
metadata:
  name: "website-static-nginx"
  namespace: stocks
spec:
  selector:
    app: stocks
    component: website
    container: static
  ports:
    - name: "nginx"
      protocol: "TCP"
      port: 80
