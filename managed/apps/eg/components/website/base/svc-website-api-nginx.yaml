---
kind: "Service"
apiVersion: "v1"
metadata:
  name: "website-api-nginx"
  namespace: eg
spec:
  selector:
    app: eg
    component: website
    container: api
  ports:
    - name: "nginx"
      protocol: "TCP"
      port: 8080
