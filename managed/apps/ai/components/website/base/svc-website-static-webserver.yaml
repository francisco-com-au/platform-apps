---
kind: "Service"
apiVersion: "v1"
metadata:
  name: "website-static-webserver"
  namespace: ai
spec:
  selector:
    app: ai
    component: website
    container: static
  ports:
    - name: "webserver"
      protocol: "TCP"
      port: 80
