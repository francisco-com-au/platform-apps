---
kind: "Service"
apiVersion: "v1"
metadata:
  name: "website-api-api"
  namespace: ai
spec:
  selector:
    app: ai
    component: website
    container: api
  ports:
    - name: "api"
      protocol: "TCP"
      port: 8080
