---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: website
  namespace: ai
spec:
  tls:
  - hosts:
    - dev.ai.francisco.com.au
    secretName: website-tls
  rules:
  - host: dev.ai.francisco.com.au
    http:
      paths: 
        - path: /
          pathType: Prefix
          backend:
            service:
              name: website-static-webserver
              port:
                number: 80
        - path: /api
          pathType: Prefix
          backend:
            service:
              name: website-api-api
              port:
                number: 8080
