---
kind: "Service"
apiVersion: "v1"
metadata:
  name: "website-static-webserver"
  namespace: bank
spec:
  selector:
    app: bank
    component: website
    container: static
  ports:
    - name: "webserver"
      protocol: "TCP"
      port: 80
