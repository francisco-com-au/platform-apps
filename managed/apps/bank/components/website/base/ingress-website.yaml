---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: website
  namespace: bank
  # annotations:
  #  nginx.ingress.kubernetes.io/backend-protocol: https # ingress-nginx
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - bank.francisco.au
      secretName: website-tls
  rules:
    - host: bank.francisco.au
      http:
        paths: 
          - path: /
            pathType: Prefix
            backend:
              service:
                name: website-static-webserver
                port:
                  number: 80
