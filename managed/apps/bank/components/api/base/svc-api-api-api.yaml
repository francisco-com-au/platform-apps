---
kind: "Service"
apiVersion: "v1"
metadata:
  name: "api-api-api"
  namespace: bank
spec:
  selector:
    app: bank
    component: api
    container: api
  ports:
    - name: "api"
      protocol: "TCP"
      port: 8080
