---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: website
  namespace: tw
  # annotations:
  #  nginx.ingress.kubernetes.io/backend-protocol: https # ingress-nginx
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - ai.francisco.com.au
      secretName: website-tls
  rules:
    - host: ai.francisco.com.au
      http:
        paths: 
          - path: /
            pathType: Prefix
            backend:
              service:
                name: website-static-webserver
                port:
                  number: 80
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: website-api-api
                port:
                  number: 8080
