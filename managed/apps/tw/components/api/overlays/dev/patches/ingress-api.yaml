---
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: api
  namespace: tw
spec:
  tls:
  - hosts:
    - api.dev.ai.francisco.com.au
    secretName: api-tls
  rules:
  - host: api.dev.ai.francisco.com.au
    http:
      paths: 
        - path: /
          pathType: Prefix
          backend:
            service:
              name: api-api-api
              port:
                number: 8080
